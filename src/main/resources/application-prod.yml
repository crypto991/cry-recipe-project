spring:
  datasource:
    url: jdbc:mysql://localhost:3306/cry_prod
    username: cry_prod_user
    password: crypto
    platform: mysql
  jpa:
    hibernate:
      ddl-auto: validate
    database-platform: org.hibernate.dialect.MySQL5InnoDBDialect
    database: mysql
    show-sql: false